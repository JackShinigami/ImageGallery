<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <item android:id="@+id/search_name"
          android:icon="@drawable/ic_search_by_name"
          android:title="@string/search_by_name"/>
    <item android:id="@+id/search_location"
        android:icon="@drawable/ic_search_by_location"
        android:title="@string/search_by_location"/>
    <item android:id="@+id/search_tag"
        android:icon="@drawable/ic_search_by_tag"
        android:title="@string/search_by_tag"/>
</menu>